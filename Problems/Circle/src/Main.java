class Circle {

    double radius;
    double getLength() {
        return 2 * Math.PI * radius;
    }
    double getArea() {
        return Math.PI * (radius * radius);
        System.out.println();
    }

    // write methods here
}